To run mail you need to do at least the following:

	cd $PLAN9/src/cmd/upas; mk install
	cd $PLAN9/src/cmd/upas/nfs; mk install
	mkdir $PLAN9/log; cd $PLAN9/log; chmod 666 smtp smtp.debug smtp.fail mail >smtp >smtp.debug >smtp.fail >mail
	mkdir $PLAN9/mail/queue; chmod 777 $PLAN9/mail/queue

